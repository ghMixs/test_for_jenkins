#!groovy
properties([disableConcurrentBuilds()])

pipeline {
    agent any
    stages {
        stage('Test') {
            steps {
                sh 'docker images ls'
            }
        }
    }
}


