#!groovy
properties([disableConcurrentBuilds()])

pipeline {
    agent any
    stages {
        stage('Test') {
            steps {
                script {
                    if (env.BRANCH_NAME == 'main') {
                        echo 'I only execute on the master branch'
                    } else {
                        echo 'I execute elsewhere'
                    }
                    
                    def inspectExitCode = sh script: "docker stop web", returnStatus: true
                    if (inspectExitCode)
                    {
                        echo 'inspectExitCode'
                    }
                    else {
                        echo 'stop'
                    }
                }
            }
        }
    }
}


