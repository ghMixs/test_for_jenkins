#!groovy
properties([disableConcurrentBuilds()])

pipeline {
    agent any
    stages {
        stage('Test') {
            steps {
                sh 'docker build -t test_nginx .'
                sh 'docker run -p 10.8.0.1:80:80 -d test_nginx'
            }
        }
    }
}


