#!groovy
properties([disableConcurrentBuilds()])

pipeline {
    agent any
    stages {
        stage('Test') {
            steps {
                docker service ls | grep jenkins && echo service-exists
                docker service ls | grep web && echo service-exists
            }
        }
    }
}


