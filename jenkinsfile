#!groovy
properties([disableConcurrentBuilds()])

pipeline {
    agent any
    stages {
        stage('Test') {
            steps {
                sh 'docker service ls | grep jenkins && echo service-exists'
                sh 'docker service ls | grep web && echo service-exists'
            }
        }
    }
}


